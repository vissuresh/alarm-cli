"""alarm-cli — a one-off alarm clock for the terminal.

A client (the ``alarm`` command) and a daemon share one JSON file and never
talk to each other directly. See docs/architecture.md.
"""

__version__ = "0.1.0"
